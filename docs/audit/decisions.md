# Decision audit — CoE_RoI_R/decisions/

Generated by `python scripts/audit_decisions.py` (structural audit; reuses the
parser in `scripts/refcheck.py`). `refcheck.py` already covers missing
localisation keys, dead event ids and unknown modifier/CB/culture names — those
are **not** repeated here. Reviewed 2026-09-06 against the 1821.9.1 start.

## Counts

| | |
|---|---|
| decision files | 71 |
| decisions | 1247 |
| disabled (`always = no`) | 16 |
| no `potential` block | 0 |
| no `effect` block | 0 |
| no `ai_will_do` | 669 |
| `ai_will_do` factor 0 | 13 |
| flagged repeatable (heuristic) | 12 |
| decisions referencing tag QNG | 19 |
| `year = 1835..1840` gates | 22 |
| exact duplicate decision names | 0 |
| tags targeted by more than one `change_tag` decision | 25 |

Reproduce: `python scripts/audit_decisions.py` (report),
`... table` (one row per decision), `... dup` (duplicate candidates).
Flag polarity matters: the script separates a *required* `has_country_flag`
from one under `NOT` — only the former makes a decision untakeable.
The report is now **[high] 0**; everything below is either fixed or a
verified false positive.

## Fixed (2026-09-06)

- `SWE.txt` `the_vega_expedition` — `has_global_flag = suez_canal_built` (never
  set anywhere) replaced with `has_global_flag = suez_canal_global`, the flag
  `decisions/Canals.txt:52` actually sets and the only Suez global flag that
  survives completion (`events/Canals.txt` clears the `building_*_phaseN`
  ones). `events/ENGFlavor.txt:2839` already tests the same flag.
  (`events/MostlyHarmless.txt:19` still tests `suez_canal_built`; out of scope
  here, left for a separate pass.)
- `India.txt` — the byte-identical second copy of `claim_greater_bengal`
  deleted.
- `the_sell_of_saint_barthelemy` — the `Desert_reclamation.txt` copy deleted;
  the `SWE.txt` one (identical apart from whitespace) kept as the topical home.
- **39 `ai_will_do` blocks added.** `factor = 1` plain, for decisions whose
  `allow` already carries the usual `war = no` / prestige / tech guard:
  `ACW.txt` form_kellys_irish_brigade; `ACW2_dec.txt` restore_the_union,
  restore_washington_capital, restore_richmond_capital; `AUS.txt`
  bosnian_annexation; `BRZ_irredentism.txt` restore_uruguay;
  `BYZ_Expansion.txt` restore_anatolia, restore_armenia_not_TUR,
  restore_armenia_TUR, restore_levant; `ENG.txt` irish_integration; `ENL.txt`
  reclaim_the_UK, reform_the_UK; `GRE.txt` restore_epirus, restore_thrace,
  restore_rumelia, restore_n_macedonia, restore_istanbul, restore_aydin,
  restore_hudavendigar; `Heavenly Kingdom.txt` claim_mandate_of_heaven;
  `KRA.txt` krakow_form_galicia, german_west_prussia; `MEX.txt`
  restore_mexican_empire; `NationalUnification.txt` restore_alsace; `SWE.txt`
  pass_folkskolereformen; `Transcaucasia.txt` form_transcaucasia; `TUR.txt`
  tanzimat_reforms, republican_reforms; `UncivFlavor.txt`
  enact_muhammad_alis_reforms, civilize_your_nation_china.
  (The GRE/BYZ `restore_*` family is province **renaming**, not conquest — the
  AI owner should undo a rival's renaming, and each already requires `war = no`.)
  `factor = 1` plus `modifier = { factor = 0 war = yes }`, for the annexation
  and claim decisions that have no `war = no` in `allow`: `ACW.txt`
  annex_hawaii; `CSA.txt` annex_cuba, annex_puerto_rico; `ENG.txt` annex_fiji;
  `France.txt` annex_mayotte, claim_savoy, annex_new_caledonia;
  `SouthAmerica.txt` annex_easter_island.
- **One-shot flags added** (`set_country_flag` in `effect`, `NOT` in
  `potential`): `BoerWar.txt` `treaty_of_vereeniging` →
  `treaty_of_vereeniging_taken`; `POR.txt` `ultimatum_to_portugal` →
  `ultimatum_to_portugal_taken` (it hands out `badboy = 4` per click);
  `China.txt` `join_the_KMT` → `join_the_KMT_taken` (AI-only; it spammed event
  131741 at KMT after every refusal).
- **`hidden_decisions` toggle**: `extra_decisions.txt` gains the one-shot pair
  `hide_debug_decisions` / `show_debug_decisions` (`ai = no`,
  `ai_will_do = { factor = 0 }`) that set and clear the flag, so the twelve
  utility decisions that test it are finally hideable. Loc keys are in
  `localisation/GVG_events.csv`. The `Tech_School_Changes.txt` six deliberately
  keep no `ai = no` — they have `ai_will_do = { factor = 1 }` and the AI is
  meant to switch tech schools; the flag is never set for AI countries anyway.
- `scripts/audit_decisions.py` — two heuristic fixes, both of which removed
  false positives rather than defects: `hist_tags()` now accepts `TAG.txt` and
  `TAG- Name.txt` as well as `TAG - Name.txt` (that is what made QNG and BIM
  look file-less), and decisions whose `potential` contains `ai = no` are no
  longer checked for `ai_will_do`.

## Verified, left alone (deferred)

- **QNG is correct as-is** — but the reasoning first recorded here was backwards
  and is corrected (2026-09-06): **QNG is the Qing and it exists at the 1821
  start**, owning 153 provinces (`grep -c 'owner = QNG' history/provinces/`),
  with `history/countries/QNG.txt`. **CHI** (`CHI - China.txt`) owns **zero**
  provinces at start; it is the later republican tag. The original "QNG has no
  history file" finding was a bug in `scripts/audit_decisions.py`, whose
  `hist_tags()` only recognised the `TAG - Name.txt` form and so missed the bare
  `QNG.txt` (and `BIM`); the heuristic now accepts `TAG.txt` and `TAG- Name.txt`
  too. The 19 QNG-gated decisions are Qing decisions and are correctly targeted;
  new Qing content must use QNG, not CHI (`events/CHIOpiumGVG.txt` does).
  `events/Taiping.txt:1348` still offers `change_tag = QNG` as a restoration
  path from the Taiping branch.
- `0_economic_decisons.txt` `reset_colony_types` — already `ai = no` in
  `potential`; it is a paid (10 000) player utility that strips
  settlement/exploitation colony modifiers, and being re-runnable as the empire
  changes is the point. `ai_will_do = { factor = 0 }` kept, no flag added.
- **Repeatable-heuristic false positives** (each verified to close its own
  guard, so no flag was added): `BYZ_Expansion.txt` restore_levant
  (`clr_global_flag = levant_renamed`); `Germany.txt` integrate_posen /
  integrate_silesia (remove the PZN / SLS cores the `potential` tests);
  `SWI.txt` declare_republic_of_neuchatel (removes the PRU/NGF/GER cores on
  608); `SWI_neutrality.txt` violate_neutrality (`ai = no`, and it removes
  SWI's `neutrality` modifier); `Political.txt` nationalize (vanilla; gated by
  the engine's `can_nationalize`); `extra_decisions.txt` leave_soi (an offer
  whose `potential` closes as soon as you are out of the sphere);
  `archaeology.txt` the five `*_excavations` — checked: events 19040/190401/…
  always end in either `add_country_modifier = *_excavations` or
  `set_country_flag = <egypt|greece|crete|maya|inca|mesopotamian>_refused`,
  both of which the `potential` tests.
- **Year 1840 exhibition / conference gates** — left at 1840 as instructed;
  none of them is a "game start" guard that needed lowering to 1825.
- `gtfo.txt:175` `gtfo_BIM` — `history/countries/BIM- Bima.txt` exists (missing
  space in the filename); no longer reported.
- Remaining low findings (`china_formed`, `dismantling_treaty`,
  `treaty_of_adrianople`, `somalia_organized` NOT-tested but never set) are
  untouched dead conditions, harmless: the condition is always true.

## Duplicate / overlapping decisions (informational)

Not defects on their own — the mod deliberately offers several routes to the
same tag — but worth a look for divergent conditions:

- become **CHI**: `China.txt` `form_china`(55), `become_leader_of_beiyang`(471),
  `become_leader_of_communists`(509), `reunification_of_china_communist`(769),
  `reunification_of_china`(982)
- become **IBR**: `SPA.txt` `create_iberia`(109)/`create_iberia2`(285) vs
  `POR.txt` `create_iberia_UPB`(495)/`create_iberia2_UPB`(652) — same union,
  two files, check the conditions match
- `India.txt`: `form_hindustan`(4)/`form_hindustan2`(1032),
  `form_marathas`(139)/`form_marathas2`(1189),
  `form_dravidistan`(302)/`form_dravidistan2`(1299) — PDM original plus a
  second GVG-era variant of each
- `NationalUnification.txt`: `form_germany`(335)/`form_germany2`(502)/
  `form_germany_AUS`(667); `form_north_german_confederation`(191)/`...2`(1086);
  `form_south_german_confederation`(3)/`austrian_...2`(104)/`...2`(1170)
- `Balkans.txt` vs `Ottoman_Dec.txt` for ROM and ALB; `KRA.txt` for POL/GLM;
  `PBC.txt` for PBC/PEU/BOL/SPU; `USCA.txt` for UCA
- `MEX.txt` `integrate_pueblo`/`integrate_texans`/`integrate_afro_caribeno`/
  `integrate_cherokee` overlap `Latin Native Integration.txt:2`
  `native_integration_mexico` — same tag, same `add_accepted_culture` effect
