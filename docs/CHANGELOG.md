# Changelog

Notable changes to the mod, newest first. One line per change; file names where they help.
Audit reports live in `docs/audit/`, design notes in `docs/design/`.

## 2026-09-06 — audit and repair pass

A read-only audit of the whole tree (countries, provinces, diplomacy, decisions, `common/`,
localisation, events, performance, AI/balance) followed by the repairs it justified, plus three
new event chains. Nothing in this pass has been tested in a running game.

### Fixes (crash / logic)

- Province 2637 was filed under Chukotka; it is Lanfang's West Borneo capital (San Kuew Jong). Moved to `history/provinces/asia/`.
- Four province ids had two history files each (1396 Api, 1397 Sandakan, 2539 Bougainville, 686 Line Islands). Kept the copy matching `map/region.txt`, deleted the `indonesia/` duplicates.
- `history/provinces/japan/1661 - Nagasaki.txt` had a `1821.1.1` block dated before the 1821.9.1 start; merged into the top-level block.
- Boer appeal event 98241 used `add_casus_belli`, which grants the CB to the *target* — Britain handed the Boers a CB against itself. Now `casus_belli`.
- Missing event 98241 (`ENG`), a brace error in the CSA `leave_slave_trade` decision, a bad cb type, five flag-name typos, four wrong loc keys and ~40 missing strings, all found by the new `scripts/refcheck.py`.
- The economy rework (99997, 999958, 27 WorkPlaceEvents dispatchers) gated on `tag = BHU`, so annexing Bhutan switched it off. Now gated on the `economy_pulse_host` country flag, elected by new event 2000100, whose trigger (`is_greater_power` + no country holds the flag) re-elects a host the day after the old one dies.
- The treasury sink in 99997 could overflow: Clausewitz parses money as int32 hundredths, so the `-50000000` effects wrapped and could pay the country. Disabled pending real thresholds.
- `events/Goods.txt` 1107-1138 were briefly dispatched from 99997 via `random_owned`, which cut their conversion rate 10-30x. Reverted to MTTH province events with cheap-first triggers.
- 2000100 now clears `economy_pulse_host` from any previous holder, so a re-election cannot leave two hosts.
- `on_actions` weights are picks, not chances — the engine fires exactly one event per country per pulse. A watchdog event (2000101) had been added to both the yearly and quarterly lists, cutting the `Canals.txt` 97200/97250/97350 rate and then halving the whole quarterly economy pulse. The watchdog and its `economy_pulse_lock` flag are gone; `on_yearly_pulse` and `on_quarterly_pulse` hold what they originally did.
- Events A-G: cleared four unknown keywords (`has_truce`, `unciv_military_industry`, `random_neighbor`, `activate_unit`), stopped 99990/31240 re-firing, repointed the China awakening chain at THIS/FROM, guarded ACW 16000, fixed the `realtion` typo and the dead `suez_canal_built` flag, lowered two bare `year = 1836` start-guards.
- Events H-Z: restored the religion filter in five empty `TemperanceLeague` OR blocks (the whole feature was dead), replaced seven empty `LiberalRevolutions` OR blocks with real building names, fixed the NobelPrize off-by-one flag, `relio`/`religion = north_german` -> `culture`, PER_crises scope wrappers, the July Revolution year gate, `modifer` -> `modifier`, and the missing Taiping 1850-1855 MTTH step.
- Decisions: `the_vega_expedition` tested a flag the canal chain never sets; two duplicate decisions deleted; three spammy repeatables closed with `_taken` flags; `hide_debug_decisions` / `show_debug_decisions` added so the debug menu is actually toggleable.
- `common/`: invalid modifier keys in `crime.txt`, `event_modifiers.txt`, `triggered_modifiers.txt` and `issues.txt`; duplicate `silk_famine` block; `CEASECOLONIZATION_DIPLOMATIC_COST` assigned twice in `defines.lua`.
- Localisation: wrong country names (ABU, CNG, KAL), mojibake in `PDM_CE.csv`, five `[Placeholder]` descriptions, real mobilize/demobilize tooltips, and four `text.csv` rows whose keys were shifted one event forward.
- `history/countries`: 9 capitals moved into provinces the tag owns, 8 undefined and 14 out-of-window `ruling_party` names repointed, HAI/KMT government fixes, 35 dominion party start dates, ETH registration, ENG -> `ENG_conservative`.
- The 11 RGO education ladder events (`+education_RGO.txt` 9999959-9999969) are `is_triggered_only` and dispatched per state from the quarterly pulse 99997 instead of being evaluated for every province every day: -117k clause-evals/day. Resync latency goes from ~8-15 days to up to a quarter; see `docs/audit/performance.md`.
- Event trigger clauses in 26 files reordered cheap-gates-first (121 triggers). A permutation of a pure predicate cannot change what fires; modelled daily trigger work drops ~22%. See `docs/audit/performance.md`.
- The six archaeology excavation ask/answer chains were province events read with `FROM` as a province, so the requesting great power was never in scope and the refusal flags landed on the wrong country. They are country events now, fired with `any_country`/`owns` from `decisions/archaeology.txt`, which also gates the Maya dig on actually owning Yucatan. Same sweep: `owner = { }` wrappers on BRZFlavor 46302 and GERFlavor 33004, `random_owned` in EconomicalEvents 22540, country-flag guards on the repeatable permanent province modifiers in 14540/22540, `major = no` on newEvents 1100132-1100143, `protector_of_eastern_christendom` made permanent and removed again by BYZ 1000205, and the missing news loc keys. `docs/audit/events-second-opinion.md`.
- English QA of all the new GVG event text (`3d3d9009`): 53 rows in `localisation/GVG_events.csv` reworked - a csv-breaking semicolon inside `EVTDESC1001802_NEWS_SHORT`, three ALL-CAPS news titles, a wrong date in the Eskinci proclamation, three over-length descriptions and 39 option buttons trimmed to fit the button. No keys or meanings changed; the full table is in `docs/audit/loc-qa-gvg.md`.
- `modcheck loc-check` now flags rows where a semicolon inside the English text splits it across columns, and summarises rows that are not exactly 15 columns wide; baselines in `.claude/skills/validate/SKILL.md`. It caught the second stray semicolon, in `EVTDESC999999` (`48a906ce`).
- `events/AUSVormaerzGVG.txt` events were reordered so the ids run 1001800-1001803 down the file (`48a906ce`).
- `common/countries/D33.txt` had `start_date = 1848.1.11860.1.1` on `dominion_communist` — two dates run together. Found by the new `scripts/audit_parties.py`; it was the only high-severity party defect in the tree.

- Four provinces were filed under the wrong continent in `map/continent.txt` (`region.txt` was right in all 12 flagged cases): 1724 Tamanrasset and 1774 Dakhla africa/asia -> east_indies (ids repurposed by the Dutch East Indies submod), 2575 Ipoh asia -> east_indies, 2589 Kanin asia -> europe. Adjacency was recomputed from `map/provinces.bmp`; the other eight regions are genuine boundary straddles. (`54ea7325`)
- `fire_only_once` is engine-wide - once per game, for one country, whatever the trigger. Eight multi-country events could therefore only ever fire once: 99916 Intervention in Germany and 97031 Intervention in Portugal (now guarded by a country flag set in every option), 810029 Immigrant Surge, 90040 Sepoy Rebellion, 34609 Mozart Festival, and the three `PORMiguelistGVG` events. The 124 remaining cases are verdicts recorded in `docs/audit/fire-only-once.md` (mostly alternative tags for the same nation, or genuine world events). (`88512c38`)
- `fire_only_once` also dropped from the new `ITARisingsGVG` Carbonari and the `PORMiguelistGVG` chain, which are per-country by design; the Carbonari intervention's relation scope was fixed at the same time.
- Small bundle (`210b0bbe`): SWHFlavor 90055 borrowed `EVTNAME90054` and now has its own title; CSAFlavor 16650/16651/16652 swapped `fire_only_once` for per-country flag guards (USA and CSA/TEX coexist after the ACW); `gtfo_PAK` / `gtfo_MLY` / `gtfo_LXA` marked `always = no` because those tags have no `add_core` anywhere, so the decisions could never appear - giving them cores is a design call; `EVTNAME70000`/`70001` restored to "$STATE$ Joins $COUNTRY$" (they fire from `on_colony_to_state` / `on_state_conquest` for any country).
- Event pacing 1821-1836 (`df58d186`): the runaway repeaters found by the new `scripts/audit_pacing.py` are guarded — 99986 (`00_CoE_RoI.txt`, Greek war support) now sets and tests a per-country flag instead of re-firing, 99800 (`POLflavor.txt`, the Kraków free city) gains the same guard, and 36996 (`ENGFlavor.txt`) gains `fire_only_once`. Chained follow-ups no longer land on the same day as their parent: `ZollvereinGVG` 1000901 +30 days and 1000902 +60 days, `RUSTurkishWarGVG` 1001402 +7 days and 1001403 +3 days, `ASHWarGVG` 1001603 +5 days. Separately, the `the_port_of_novorossiysk` decision in `decisions/RUS.txt` tested a global flag nothing sets (`treaty_of_adrianople`); it now reads the real `adrianople_treaty`. What was checked and deliberately left alone is recorded in `docs/audit/pacing-1821-1836.md`.
- Integration review (`48e82466`): the `audit_events.py` baseline in `.claude/skills/validate/SKILL.md` still claimed 2 highs after 14540 and 22540 were fixed, which would have masked a real regression; it is 0 highs now, and the event-id total is drift-tolerant with only the duplicate count kept as an invariant.
- French Algiers chain (`9ef724f6`, `events/FRAFlavor.txt`): the 1830 Fan Affair 37234 was guarded by `truce_with = ALD` but not `war_with`, so it could fire while France was already at war with Aldjazair and issue a second declaration; and the Algerian Rebellion 37235 read a country flag `algerian_rebellion` that nothing ever set, leaving its 4x slowdown dead and the event on an endless 3-month respawn. The crush option now sets the flag.
- `history/wars/OttomanPersianWar.txt` was dated `1821.9.10`, nine days *after* the bookmark, so the war it adds never existed in a 1821 game — history is only read up to the start date. Now `1821.8.1`. (`9a5ddb12`)
- Review follow-ups on the two newest chains (`970cad5b`, `0b155b56`): `JAPTenpoGVG` gained a real `rangaku` modifier, namespaced its flag and can no longer stack the Tenpo famine twice; `BRZFlavor` 46316 is guarded with `brazil_majority` so the Regency and the majority declaration cannot both resolve, and Belem's ownership is documented in `docs/design/brazil-regency.md`.
- Ayacucho reviewer fixes (`03678465`): the triggers now account for DOM, use colony capitals, and handle the case where Spain misses the 1824 offer.
- Crisis system (`0079f69a`): two `ai_chance` dead ends in `events/crises.txt`. The "work with them" option of the Native-American flashpoint event 20110 was `factor = 0` for theocracies, absolute monarchies and Prussian constitutionalism, so an autocrat at 23+ infamy had *both* options weighted 0 and the infamy guard on the massacre option never engaged (now 0.1); and the "decline crisis interest" option of vanilla 20000 was `factor = 0`, i.e. unreachable for the AI (restored to vanilla's implicit 1).
- Great Game year gate (`events/TheGreatGame.txt`): the ENG/RUS influence-and-CB framework over Sind, Baluchistan, Afghanistan and the khanates had no year gate at all - only `is_greater_power` and a government check - so it could open in 1821, fifteen years early. The three self-firing entry events 95610 (ENG), 95611 (RUS) and 95612 (the target's side) now require `year = 1828`, the Turkmenchay settlement that actually put Russia on the Persian-Afghan frontier. The chained events (95613-95616) are untouched. Found by `docs/audit/bridge-1836.md` item 2.
- Crisis suppression (`8755d326`): `events/crises.txt` 20111 gated its claimant test on `ai = yes`, so a claim held by the human player did not count and the flashpoint tension it was meant to drain kept building only against the AI. The test is symmetric now. In the same pass the commented-out `country_event = 2626` dispatch was investigated and left disabled on purpose — it is the mod's only `remove_core` path, and re-enabling it needs the `FROM`-scope clause in `CBsAndCores.txt` verified in a running game; the reasoning is recorded in `docs/audit/crises.md`.

- `events/FRAFlavor.txt` line-by-line review (`a34858ce`): the crushed July Revolution still set `july_revolution`, so the cascade keying off it fired even when Charles X won; a Corsica `has_pop_culture` test was in the wrong scope; two options pointed at other events' loc keys; a dead `mean_time_to_happen` ramp; and a relation effect past the engine clamp. Report: `docs/audit/legacy-FRAFlavor.md`.
- `events/ENGFlavor.txt` line-by-line review (`8595d290`): 36936 fired on the wrong tag, 36984 chained to the wrong follow-up id, 36909 read a flag in the wrong scope, plus wrong option loc keys and a duplicated block. The `NOT` splits it also flags are cosmetic only - multi-statement `NOT` is `NOR`. Report: `docs/audit/legacy-ENGFlavor.md`.
- `events/RUSFlavor.txt` line-by-line review (`e7ff087e`): the Alaska sale 32510 was a dead end, crushing the November Uprising set Russia's ruling party to a liberal one, the Kenesary chain 375008 cancelled itself, plus relation and badboy effects past the engine clamps. Report: `docs/audit/legacy-RUSFlavor.md`.
- `events/AUSFlavor.txt` line-by-line review (`ac304dd7`): 98007 punished conceding a free press harder than refusing it, two options pointed at the wrong loc keys, and 90023 handed out a prestige `-100`. Report: `docs/audit/legacy-AUSFlavor.md`.
- `events/USAFlavor.txt` line-by-line review (`a0ff9d44`): malformed war goals in the Mexican-American War event 44127, a wrong option loc key, and effects past the engine clamps. Report: `docs/audit/legacy-USAFlavor.md`.
- `events/Ottoman_Event.txt` line-by-line review (`9a013cbe`): 31252 and 31253 deleted the Ottoman cores on Crete and Cyprus, a stray `leave_alliance`, and the Greek Question offer 31264 could repeat. Report: `docs/audit/legacy-Ottoman.md`.
- `events/PRUFlavor.txt` / `events/GERFlavor.txt` line-by-line review (`d277fd54`): 34607's MTTH ramp sat outside its own 1856-1859 window and duplicated a consciousness block, GERFlavor 33031 asked for a relation `+400` (clamped at 200) and 33020 chained on a dead `tag = AUS` branch where `decisions/Germany.txt` uses KUK. Report: `docs/audit/legacy-PRU-GER.md`.
- `events/SPAFlavor.txt` line-by-line review (`20674dea`): 37708 and 37740 displayed other events' text, 37702 and 37708 had MTTH ramps outside their own windows, 37711 zeroed `christino_provinces` immediately after setting it, 37743 carried three MTTH modifiers its trigger excludes, 97151 declared war for free where its twin charges infamy, and 37762's "let Spain be" option thanked every European power because an `OR` of two `NOT`s is always true. Report: `docs/audit/legacy-SPAFlavor.md`.
- `events/NationalUnification.txt` line-by-line review (`22ba8aab`): the NGF core cession, a coercion effect in the wrong scope, bad culture triggers and an unguarded Yugoslav offer. Report: `docs/audit/legacy-NationalUnification.md`.
- `events/PoliticalEvents.txt` line-by-line review (`e4c25272`): 4100 option B pointed at a planned-economy loc key that contradicted its own effect, 3102 and 3752 had aristocrats search-replaced with capitalists (dead `OR` branches and pop effects applied at double magnitude), and 4412's MTTH used the pop-scope `consciousness` trigger at country scope. Report: `docs/audit/legacy-PoliticalEvents.md`.
- **Correction** (`c75a129c`): part of the PRU/GER, NationalUnification and FRAFlavor pass had "fixed" `has_pop_religion = north_german` into `has_pop_culture`. That was wrong — the German and Italian sub-cultures live in the pop *religion* field (1138+ `religion = north_german|south_german` in `history/pops/1821.9.1`, zero live `culture =`), and `common/religion.txt` appends the culture list so they resolve as religions by design. Reverted NationalUnification 620/627/967/983, PRUFlavor 34601/34608 and FRAFlavor 37226 (Corsica); `TemperanceLeague.txt` stays commented out as originally, since no pop in the mod has a real `catholic`/`protestant` religion.
- `events/RUSTurkishWarGVG.txt` 1001401: the "March on Constantinople" option soothed pops with `limit = { has_pop_religion = orthodox }`, which matches nothing (see the correction above — no pop carries a real religion). Now a culture limit on `russian`/`byelorussian`/`ukrainian`.
- `events/ITAFlavor.txt` line-by-line review (`a2e52509`): two royal-standard events ran country effects at province scope, the Plombieres chain and province 35311 were hard-coded to `SAR` where the mod's tag is `SVY`, the Sardinian union was gated on `nationalism_n_imperialism` (unreachable at its own date), and 35310 was hoisted so the chain reads in order. Report: `docs/audit/legacy-ITAFlavor.md`.
- `events/LiberalRevolutions.txt` line-by-line review (`4e56e9fb`): 10221 used `THIS` where the scope was already the actor, 10225 tested a dead culture (now `has_pop_religion`, the sub-culture form this mod actually uses), 10150 named factory tiers that do not exist, and 10110 applied an effect in the wrong scope. Report: `docs/audit/legacy-LiberalRevolutions.md`.
- `events/China.txt` / `events/CHIFlavor.txt` line-by-line review: relation effects past the engine clamp fixed; the medium-severity items are documented rather than changed, in `docs/audit/legacy-China.md`.
- `events/JAPFlavor.txt` line-by-line review (`7fddaf2b`): 97641 referenced a reform group that does not exist, 97680 set a dead-end flag and had an MTTH that could never resolve, 97647 and 97652 called `release_vassal` in the wrong scope, plus wrong option loc keys. Report: `docs/audit/legacy-JAPFlavor.md`.
- Religion stage 1 (`a3a51224`): `carlist_rebels` could never spawn - its `spawn_chance` carried a `factor = 0` under an always-true `NOT`, so the Carlist rising was mechanically impossible. Same pass: the always-true `limit` blocks in the Dungan content, and two stray pops still carrying a real religion. The wider question stays deferred; see `docs/audit/religion-dead-content.md`.
- `events/Taiping.txt` line-by-line review (`efb3c2b6`): the AI Qing had a 50% chance of *becoming* the Taiping, which orphaned every QNG trigger downstream, 160025 handed out an accepted culture that was never removed, and several militancy effects were past the engine caps. Report: `docs/audit/legacy-Taiping.md`.
- `events/Scramble for Africa.txt` / `New Colonies.txt` line-by-line review (`9a316fa9`): 95505's effect was stranded inside a `limit`, 95514 left a gap in the colonisation ladder, 95507 carried duplicate war blocks, and 98900 had an unconditional `mean_time_to_happen` factor. Report: `docs/audit/legacy-Scramble.md`.
- `events/BoerWar.txt` / `NdebeleGazaWar.txt` line-by-line review (`ceee89de`): the Natal annexation arc was dead because it called `release` instead of `release_vassal`, SAF formation was gated on a flag only an abandoned event set, the releases fired in the wrong order, and `claim_xhosa_lands` pointed at ENL. Report: `docs/audit/legacy-BoerWar.md`.
- Integration review 5 (`65033ca9`): the Greek Question offer 31264 still relied on engine-wide `fire_only_once`, so only one Ottoman game in the world could ever see it; replaced by a per-country flag. Report: `docs/audit/legacy-Ottoman.md`.
- `events/Revolution_*.txt` line-by-line review (`7d35ce80`): a dead `2nd_republic` branch, `former_overlord` never cleared, countries inviting themselves to their own summits, a vassal call-for-help `AND`/`OR` mix-up, a cost mismatch, a pacifism MTTH typo and 800095 going to the wrong recipient. Report: `docs/audit/legacy-Revolution.md`.
- `events/BritishDominions.txt` / `CANFlavor.txt` line-by-line review (`29b50024`): 18 blocks hard-coded `ENG` where the scope was `THIS`, the Canadian chain was unreachable for ENL, a `change_controller` applied worldwide instead of to the rebel provinces, stray Alaska cores and a missing New Zealand tag gate. Report: `docs/audit/legacy-Dominions.md`.
- `events/India.txt` / `Sepoy rebellion.txt` line-by-line review (`1bac51cd`): engine-wide `fire_only_once` used inside `any_country` loops, a dead Barrackpore escalation, the `sepoy_rebellion` flag stranded on countries that could never clear it, a syntax error in 97115 and 97125 re-firing monthly. Report: `docs/audit/legacy-India.md`.
- `events/MEXFlavor.txt` / `BRZFlavor.txt` line-by-line review (`e661839d`): 44854 looped on the Cherokee resettlement, and the Farroupilha used `release` where the chain needs `secede_province`/`release_vassal`. Report: `docs/audit/legacy-MEX-BRZ.md`.
- `events/CBsAndCores.txt` line-by-line review (`3ea88292`): 2616 granted the CB to the wrong side and let a country select itself as the target, 2530 was a self-relation no-op, 2612 lacked a union-core guard, and 2670's `ai_chance` ladder was cumulative. Report: `docs/audit/legacy-CBsAndCores.md`.
- `events/ColonialUprisings.txt` / `CivilizationAndGunBoats.txt` line-by-line review (`3223bd57`): 14670's badboy cost 15 -> 8, 13021's vetoes sat on the wrong option, 13016's text was wrong and 13065's relations effects were off. Report: `docs/audit/legacy-Colonial.md`.
- `events/Indochina.txt` line-by-line review (`def749e4`): three option titles pointed at neighbouring options' loc keys, `treaty_of_yandabo` was dead once BUR fought on, the Cochinchina campaign had no year gate, and 95645's text was wrong. Report: `docs/audit/legacy-Indochina.md`.
- Integration review 6 (`bc1cc97f`): corrected the Revolution report and refreshed the baselines in `.claude/skills/validate/SKILL.md`. Reports: `docs/audit/legacy-Revolution.md`.
- `events/GreatWar_Events.txt` line-by-line review (`13962b2a`): the whole Great War chain was unreachable - the entry event 96000 required a great power already carrying `in_great_war`, a flag only 96000 itself ever sets, so nothing could bootstrap it; 96065/96066 handed the `dismantle_cb` to the event's own owner; and 96021's exclusion list was missing CAN, DNB, HUN and TKG. Hardened since: 96000's top-level trigger now also tests `NOT = { has_country_modifier = no_more_war }`, so a great power sitting under its own post-war cooldown can no longer initiate one (the enemy-side check was already there). Report: `docs/audit/legacy-GreatWar.md`.
- `events/POLflavor.txt` line-by-line review (`435d43d6`): the `become_poland` decision fired the "Future of Poland" chain as the first statement of its effect, before `change_tag = POL`, so every `POL = { country_event = ... }` hop in it was a no-op; the chain now fires last and routes its follow-ups on the root. Same pass: 99800 handled a PLC actor inheriting Kraków, and the propose/refuse pair could loop. Report: `docs/audit/legacy-POLflavor.md`.
- `events/ExtraElectionEvents.txt` line-by-line review (`89ca7970`): an `anit_industrial` typo made several election modifiers silent no-ops, two slavery election events carried no `issue_group` so they were never drawn, plus wrong-signed option magnitudes. Report: `docs/audit/legacy-Elections.md`.
- `events/Diseases.txt` / `NaturalDisasters.txt` line-by-line review (`ec5d8f92`): the Krakatoa eruption wiped ~90% of a province's pops, and the influenza `mean_time_to_happen` was inverted so it was likeliest where the outbreak was least plausible. Report: `docs/audit/legacy-Disasters.md`.
- `events/EconomicalEvents.txt` line-by-line review (`62938030`): the bull-market chain re-armed itself into a repeating money loop; 22610/22616 now carry guards. Report: `docs/audit/legacy-Economical.md`.
- `events/ACW.txt` line-by-line review (`5149a4b3`): the early-abolition path let the South secede in the 1820s (now gated to 1855+), the Trent Affair's `ai_chance` had the AI always escalating, plus a wrong option loc key, a dead MTTH modifier and 16440's `reduce_pop` magnitude. Report: `docs/audit/legacy-ACW.md`.
- `events/Scandinavia.txt` line-by-line review (`2a780868`): 95095 rewrote *Sweden's* government whoever the Norwegian overlord was, the DAN events read `dominant_issue` at country scope, plus wrong option loc keys and a badboy effect past the engine clamp. Report: `docs/audit/legacy-Scandinavia.md`.
- `events/NETFlavor.txt` / `BELFlavor.txt` line-by-line review (committed inside `3def5651` "Refresh audit reports"): the `signed_treay_of_london` typo across four events and NET history, PDM's London Conference 36725 now guarded against the `BELRevolutionGVG` prelude, 36740's refusal no longer partitions Belgium anyway, guards on 36725/36738/36720 and `ai_chance` weights. Report: `docs/audit/legacy-NET-BEL.md`.
- Generic government events (`cce92e6a`): Dictatorship 20001 stacked a permanent consciousness modifier on every province each time it fired, several MTTH modifiers read dead flags, the UpperHouse options showed the wrong text, and a Monarchy event ran in the wrong scope. Report: `docs/audit/legacy-Government.md`.
- Ideology enablement and stronghold events (`b5697ee7`): the stray `Socialism_Facism.txt` duplicate removed, a core guard added to 880050 and limits to 17620. The socialist/communist enablement-date inversion and the dead states in events 1001/1002 are documented as design questions instead. Report: `docs/audit/legacy-Ideology.md`.
- `events/WorkPlaceEvents.txt` line-by-line review (`caf133f3`): all 34 dispatchers wrote the `futile` marker *before* the sub-event they fired could read it, so ~60 payload blocks never matched; 30 empty `OR` blocks left by the factory-name collapse kept six dispatchers from ever firing; plus province-scope effects wrapped in `random_owned`. Report: `docs/audit/legacy-Workplace.md`.
- `events/CrimeAndPunishment.txt` / `Political Scandals.txt` line-by-line review (`b8e81752`): 22070's two options were identical and net-zero, 22050 ran state-scope effects at the wrong scope, and the 880200 scandal flags were never cleared. Report: `docs/audit/legacy-Crime.md`.
- `events/SuffragetteMovements.txt` / `IssueSuggestion.txt` line-by-line review (`ce14d6ff`): 3301's options pointed at the wrong loc keys and 3006's reform effect was a no-op. Report: `docs/audit/legacy-Suffrage.md`.
- `events/DNBFlavor.txt` line-by-line review (`b4a97cd9`): `has_country_flag` was used as an effect in four places, so those flags were never set, and the agitation effects were stamped on foreign-owned cores. Report: `docs/audit/legacy-DNBFlavor.md`.
- `events/PERFlavour.txt` line-by-line review (`772fc38b`): a CB duration given in years where the engine reads months, the Persian-victory `secede_province` run at the wrong scope, AZB released as a vassal of the wrong country, and chain flags left uncleared. Report: `docs/audit/legacy-PERFlavour.md`.
- Latin America line-by-line review (`e13cb7f1`, `events/USCAFlavor.txt` / `SouthAmericaFlavor.txt` / `ARGFlavor.txt` / `CLMFlavor.txt`): the Panama secession targeted NIC, a Belize event fired at itself, the Entre Ríos event was dead because it routed via NOR, `vote_franschise` was used as an effect, and `join_mexico` sat on both options. Report: `docs/audit/legacy-LatinAmerica.md`.
- `events/PORFlavor.txt` line-by-line review (`b4a1ee79`): 97031 aided a POR that does not exist from 1821, `military_industry` used as an effect, 97031's IBR guard missing from 97029, the Gaza CB granted in the wrong direction, and references to dead regions. Report: `docs/audit/legacy-PORFlavor.md`.
- GRE / KOR / ARA / CSA / SWH line-by-line review (`43e17430`): the London Conference rewards were keyed on flag names nothing sets, 31206 needed `OR` where it had `AND`, the Korean chain required CHI instead of the QNG overlord, and several CBs were self-targeted. Report: `docs/audit/legacy-GRE-KOR-ARA.md`.
- Ideology enablement inversion (`events/IdeologyEnabling.txt`): the early-unlock chain enabled `communist` at event 1000 and `socialist` at 1001, while `common/ideologies.txt` auto-enables socialist 1860 / communist 1865, so 1001 could never fire from 1860 on. The chain was swapped back to the vanilla order (1000 -> socialist, 1001 -> communist gated on socialist), which also matches the mod's own localisation (`socialist;Social-Democrat`) and its invention gates (`karl_marx` then `vangaurd_party`). `common/ideologies.txt` was deliberately left untouched, so no party activation date moved and no party block needed adjusting (`audit_parties.py` identical before and after). Report: `docs/audit/legacy-Ideology.md`.
- The triggered modifier `money_hoarder_1..4` (and 111 other definition names) displayed its raw script identifier in game because no csv defined it. New `scripts/refcheck.py defs` check lists every player-visible definition name without a localisation key (modifiers, national focus, issue options, ideologies, governments, goods, buildings, factory production types, rebel types, casus belli, crime, cultures, religions, pop types, tags, technologies, inventions), each convention read off the vanilla files. All 115 findings filled in `localisation/00_CoE_RoI_definitions.csv`; the check is now part of the default run and of the `/validate` baseline at 0.

### Balance — **UNTESTED IN GAME**

- `common/production_types.txt`: the five factory templates' `efficiency` blocks are *maintenance* goods, bought daily per level and summed with `input_goods`. They were tuned against a price table 20-200x cheaper than the current `common/goods.txt`, so heavy (0.26x), light (0.15x) and food (0.21x) factories could not break even at any employment level — including the 376 `heavy_factory` levels that exist at the 1821 start.
- Each block is scaled by a single per-template factor to land at a 1.35x revenue/(input+maint) ratio at base prices. No `value`, `input_goods`, owner, employee or workforce entry moved. Also `military_factory` `heavy_industry` 35 -> 20, `luxury_factory` `light_industry` 22 -> 10 with `value` 25 -> 29, and `artisan_horsebreeder` relaxed to 2.42x because artisans are the world's only source of horses.
- Expected effect: the AI's industry is solvent from day one and stops ranking military/luxury factories 10x above everything else; raw-goods demand falls hard (those 376 heavy levels bought ~154,000/day of coal, iron, copper, lead, sulphur, timber, oil, rubber and horses, now ~16,800).
- Watch in a play test: RGO stagnation and collapsing raw prices (if so, raise `value` on the raw tiers rather than re-inflating maintenance); factory profitability after 1840, once `heavy_industry`/`light_industry` trade above base; `food_factory`, which still emits 400 units of a good priced 0.5 and will track `food_industry`'s price to the floor; whether artisans still crowd out factories.
- Method, before/after table and the solver (`scripts/balance_factories.py --target 1.35`) are in `docs/design/factory-balance.md`.
- Start-state tech outliers: AUS 14 -> 19 techs, CSA 49 -> 22 (the USA's list), 13 unformed release tags 24 -> 14, SPC prestige 50 -> 0.
- Event spam guards restored on `GreatPowers.txt` 800004/800006 and `00_CoE_RoI.txt` 99988.
- `common/defines.lua` restored to four real-world / vanilla values (owner decision): `INFAMY_STATUS_QUO` 1 -> 0 (a white peace is free again), `BADBOY_LIMIT` 50 -> 25, `MAX_BUREAUCRACY_PERCENTAGE` 0.001 -> 0.01 and `BUREAUCRACY_PERCENTAGE_INCREMENT` 0.000 -> 0.001 (admin efficiency responds to bureaucrats and to social administrative reform again). Nothing else was retuned: halving the infamy limit doubles the bite of every absolute `badboy = N` grant and halves the infamy at which every fractional `badboy = 0.8`-style trigger fires, so the affected script is listed under "Follow-ups after the defines change" in `docs/audit/common.md` rather than edited - the 1848 German revolution options (`badboy = 40` to `150`), the ten BYZ_Expansion `ai_will_do` thresholds written as absolute infamy, `InfamyWar_Events.txt`, and the `admin_found_*` bureaucrat ladder in `triggered_modifiers.txt`, whose tiers all sat above the old cap.
- `common/defines.lua` `LITERACY_CHANGE_SPEED` 0.0050 -> **0.05** (half of vanilla's 0.1; the old value was 1/20 of vanilla). With historical 1821 start literacy the old rate would have frozen Russia, the Ottomans and every unciv at their start value for the whole game. Calibrated against Russia ~6% (1820) -> ~28% (1897) and Prussia 55% -> ~99% (1900); held at half vanilla because the mod already grants a flat `education_efficiency_modifier = 2.00` in `base_values`. Play-test check: sample literacy in 1840/1860/1900 for RUS, TUR, QNG, PRU and ENG; if it runs away, 0.02-0.03 is the next step down. Reasoning in `docs/design/literacy-1821.md`.

### New content

- **Belgian Revolution prelude**, `events/BELRevolutionGVG.txt` ids 1000301-1000308: Brussels riots after the July Revolution, crack down or concede, the Provisional Government (recognise / fight / appeal to the Holy Alliance), French and British reactions, the Twenty-Four Articles, Leopold's accession, and an 1839 Treaty of London fallback, handing off to PDM's existing London Conference (36720, now gated to 1830 and to `BEL_revolt_in_progress`). No new provinces, modifiers or pictures. `docs/design/belgian-revolution.md`.
- **Decembrist revolt**, `events/RUSDecembristGVG.txt` ids 1000400-1000403: the death of Alexander I and the interregnum, the rising on Senate Square (crush / negotiate / let Constantine reign, with a news article), then the Third Section and Pestel's *Russkaya Pravda*. Fills the 1821-1836 gap; `RUSFlavor.txt` only starts in 1827. One new modifier, `nicholas_reaction`. `docs/design/decembrist-revolt.md`.
- **Greek kingdom**, `events/GREKingdomGVG.txt` ids 1000500-1000502: the London Protocol crown offer, Otto of Wittelsbach landing at Nafplion under the Bavarian regency, and the 3 September 1843 revolution (mutually exclusive with the `hellenic_parliament` decision via its `voule_ton_ellinon` flag). One new modifier, `bavarian_regency`. `docs/design/greek-kingdom.md`.
- **English Age of Reform**, `events/ENGReformGVG.txt` ids 1000600-1000602: the Clare election and Catholic Emancipation 1829 (`minorities_reform` limited -> protected), the 1830-32 Reform Crisis triggered by the July Revolution, and the Days of May if the Lords throw out the Bill (`vote_franschise` -> `wealth_voting`, the value ENG's own 1836 history block uses). Pop effects are filtered to the home islands; the chain's flags are read by `Irish woes.txt` 1010020 and `LiberalRevolutions.txt` 10330. No new modifiers or pictures. `docs/design/eng-reform-era.md`.
- **Auspicious Incident**, `events/TURAuspiciousGVG.txt` ids 1000700-1000702: the Eskinci mutiny of June 1826 with a crush/back-down branch, the Mansure Army follow-up, and a second chance in 1831. Two new modifiers (`nizam_i_cedid`, `janissary_ascendancy`) and an additive `ai_will_do` hook on `tanzimat_reforms` in `decisions/TUR.txt`. `docs/design/auspicious-incident.md`.
- **November Uprising repair**, `events/RUSFlavor.txt`: 95070 was gated `year = 1828` with a 48-month MTTH, so the rising could fire any time from 1828. Re-gated to 1830.11-1832 at a 1-month MTTH, and new 95073 is the Organic Statute settlement — it also fires after a white peace, inherits CPL on the abolition option, and only stacks `nicholas_reaction` when the Decembrist copy is not already running. The CPL vassalage in `history/diplomacy/PuppetStates.txt` no longer lapses in 1832.
- **Zollverein**, `decisions/ZollvereinGVG.txt` + `events/ZollvereinGVG.txt` ids 1000900-1000904: a `found_the_zollverein` decision for Prussia (1833+, at peace, prestige) that offers accession to every eligible German minor outside Austria's sphere, the accept/refuse branches, Vienna's reaction ten days later, and the completion of the union — gated on a `zollverein_founder` country flag so it survives Prussia becoming NGF/GER. Plus a cheap `form_the_southern_customs_union` counter-decision for Austria, reusing the existing `customs_union` modifier. Two new modifiers (`zollverein_member`, `zollverein_leader`). `docs/design/zollverein.md`.
- **Miguelist / Liberal Wars**, `events/PORMiguelistGVG.txt` ids 1001000-1001003: continues `PORFlavor.txt` 97030 (whose two options now set `por_charter_granted` / `miguelist_usurpation`) with the Charter and Miguel's usurpation, the liberal regency at Terceira and the convention of Evora Monte 1834. The civil war is fought by engine rebels via pop ideology and militancy, following the Carlist model in `SPAFlavor.txt` 37711/37712 — no new tag, war or release. `docs/design/miguelist-wars.md`.

- **Italian risings 1831**, `events/ITARisingsGVG.txt` ids 1001100-1001104: a Carbonari undercurrent for the Italian minors from 1821, Ciro Menotti's Modena conspiracy after the July Revolution, the revolt of the Papal Legations in February 1831, the Austrian intervention as an AUS choice (march over the Po vs let it burn) and the great-power reaction. No new tag or war; the risings are fought by engine rebels. `docs/design/italian-risings-1831.md`.
- **USA sectional crisis 1823-1833**, `events/USASectionalGVG.txt` ids 1001200-1001202: the Monroe Doctrine, the Tariff of Abominations and the Nullification Crisis. The Monroe event sets `monroe_doctrine2` on American-capital countries, which makes the long-dead Chilean event 198260513 reachable for the first time. `trade_policy` is a party issue and cannot be set by effect, so the tariff is a country modifier. ACW 16000 gets two `mean_time_to_happen` clauses reading the new flags; its trigger is untouched. `docs/design/usa-sectional-1820s.md`.
- **Java War 1825-1830**, `events/JavaWarGVG.txt` ids 1001300-1001302: Diponegoro's rising against NET, the capture at Magelang five years later with the Cultivation System as the pay-off, and a conciliatory branch that buys off the Javanese regents instead. Two new event modifiers (`java_war` province, `cultuurstelsel` country). Java is enumerated by province id (1413-1421) because region `NET_1413` also contains three Saharan-named provinces. `docs/design/java-war.md`.
- **Russo-Turkish War 1828-29**, `events/RUSTurkishWarGVG.txt` ids 1001400-1001403: the Tsar's 1828 ultimatum (war with a `cut_down_to_size` goal, or diplomatic pressure), the Treaty of Adrianople in 1829 (moderate peace, or the march on Constantinople with a great-power reaction) and an Ottoman mirror event for the peace. Per-country flag guards throughout. The chain stands down as soon as the PDM `treaty_of_adrianople` decision sets `adrianople_treaty`, and its moderate peace sets `london_conference_1832_held` only when no conference is running, so `GREKingdomGVG` can still fire. `docs/design/russo-turkish-1828.md`.
- **Qing opium edicts 1821-1839**, `events/CHIOpiumGVG.txt` ids 1001500-1001502: the Daoguang prohibition edicts, the 1830s silver drain (debase / crack down / legalise, the legalisation option re-adding `canton_squeeze` after clearing it) and a British Canton-trade reaction. It does not duplicate the Opium War - it only adds `mean_time_to_happen` clauses to the existing Kowloon incident (`CHIFlavor.txt` 1316081) reading the new flags. Fires on **QNG**, the Qing tag that actually exists at the 1821 start. `docs/design/qing-opium-edicts.md`.
- **First Anglo-Ashanti War 1823-1831**, `events/ASHWarGVG.txt` ids 1001600-1001603: MacCarthy's expedition up the Pra, his death at Nsamankow in January 1824 seen from the British side, the Ashanti side of the same victory, and the 1831 settlement that fixed the Pra as the border. `docs/design/anglo-ashanti-1824.md`.
- **Austrian Vormärz 1822-1835**, `events/AUSVormaerzGVG.txt` ids 1001800-1001803: the Metternich system after the Congress of Verona, the Hungarian Diet of 1825 (concede the constitution or rule by decree), Széchenyi's Academy and the Hungarian reform current, and the death of Francis I in 1835 with the Staatskonferenz regency for Ferdinand. Two new event modifiers, `metternich_system` and `kolowrat_administration`. `docs/design/austria-vormaerz.md`. (`8b5e2d6d`)
- **Brazilian Regency 1831-1840**, `events/BRZRegencyGVG.txt` ids 1001900-1001903: the abdication of Pedro I in April 1831, the Regency and its choice of a repressive or a decentralising course, the Cabanagem in Para 1835-1840, and the declaration of Pedro II's majority in July 1840. One new event modifier, `brazilian_regency`; the Farroupilha (`BRZFlavor` 46325 and `history/wars`) is untouched. `docs/design/brazil-regency.md`. (`1a499fcd`)
- **Japan, the Tenpo era 1825-1841**, `events/JAPTenpoGVG.txt` ids 1002000-1002004: the 1825 uchiharai (no second thought) expulsion edict, the Siebold incident, the Tenpo famine and the Osaka rising of Oshio Heihachiro, the Morrison incident and the Tenpo Reforms of 1841. It fires on **TKG**, the Shogunate tag that holds Japan at the 1821 start; `history/diplomacy/Japan.txt` makes JAP and the han its substates, so bakufu policy belongs to TKG. `docs/design/japan-tenpo.md`. (`87994a83`)

- **Ayacucho and the loss of the Americas 1824-1834**, `events/SPAAyacuchoGVG.txt` ids 1002100-1002102: the three Spanish American wars live at the 1821 start (Peruvian, Colombian, Dominican) had no scripted way out — the colonial goals are `make_puppet` / `cut_down_to_size`, and `cut_down_to_size` has a `peace_cost_factor` of 100, so the AI grinds on for decades. 1002100 offers Spain the historical settlement after Ayacucho (9 December 1824), 1002101 is the American side of recognition, and 1002102 is a forced peace from 1827 once the treasury and army are spent if she refuses. `docs/design/ayacucho-1824.md`. (`f2a25551`, `03678465`)
- **Massina Jihad resolution**, `events/WarResolutionsGVG.txt` ids 1002200-1002201: the same `cut_down_to_size` trap froze `history/wars/MassinaJihad.txt` (live since 1818) — one-province Massina can never buy the goal. Seku Amadu now gets the historical settlement from 1843, with a forced peace if he refuses. The new `docs/audit/start-wars.md` audits all eight wars live on 1821.9.1 and finds the other six are endable by the AI or already scripted. (`e6749a44`)
- **First Anglo-Afghan War 1838-1845**, `events/AFGWarGVG.txt` ids 1002300-1002303: the Simla Manifesto and the march on Kabul, Shuja on the throne, the rising of November 1841 and the destruction of the Kabul garrison in the retreat to Jalalabad, the Army of Retribution in 1842 and Dost Mohammad's restoration, closing with the 1845 settlement. It was the last gap left by `docs/audit/bridge-1836.md` item 2 and is no longer deferred; the effects are scoped to the countries that actually own the land (`69aebd24`, `3b1770e0`).

### Start-state history (1821.9.1)

- Bavaria is a constitutional monarchy, not an absolute one — the 1818 constitution gave a bicameral Landtag with an elected lower house. `BAYFlavor` 33401/33402 already accept it.
- Egypt starts as an Ottoman vassal from 1805 (`PuppetStates.txt`), and because a vassal cannot lead its own war the ongoing Egyptian conquest of Sudan was undefined behaviour. Sennar fell in June 1821 and Kordofan in August, so SUD's 17 provinces now start owned and controlled by EGY (SUD/DAR/ETH/TIG cores kept) and `history/wars/EgyptianConquestofSudan.txt` is deleted. SUD is still released later by the ARAFlavor chain.
- Peruvian War of Independence: attacker/defender sides were inverted. Now PEU/ARG/CHL vs SPA, with matching teardown and `cut_down_to_size` goals; the unresolvable `liberate_country BOL` goal is gone.
- New `history/wars/OttomanPersianWar.txt` (1821-1823, PER vs TUR, `status_quo`).
- Alliances: four impossible ECU/BOL pairs deleted, GCO/BOL re-dated to 1825, and the Quadruple Alliance (ENG with AUS/PRU/RUS) plus the Anglo-Portuguese alliance (ENG-UPB) added.
- Xhosa and Farroupilha wars re-dated; the Ottoman restoration of Tripoli kept and dated.
- The 39 country history files whose tag was never listed in `common/countries.txt` are now registered (22 US states UAL-UWV, plus BMK, DUR, ERT, KRL, KYR, LNA, LUZ, MAN, OAX, PML, SHI, SIE, SMI, SNG, SNZ, SON, UZB): they had `common/countries/*.txt` and full flag sets already, so registration was the only missing piece. Localisation for the 22 state tags and their party names added to `CountriesGVG.csv`; seven map colours de-clashed from neighbours (BMK/KRT, DUR/AFG, UZB/TKS, MAN/UNY, KRL/TKM, UAL/HAI, OAX/UPB); `LUZ_conservative` back-dated to 1820 and the AFG/KRL conservative windows extended so a ruling party exists all game; SON's ruling party moved to `SON_conservative` (`presidential_dictatorship` forbids liberals). `audit_countries.py` is at 0 highs.
- Whole pass documented in `docs/audit/start-state-1821.md`: SPA (Trienio), UPB (Vintista), SIC, SAR, FRA, PRU, AUS and RUS were already correct, GRE deliberately stays absolute, PER `civilized = yes` is deliberate.
- Vienna settlement spheres and guarantees (`docs/design/vienna-settlement-1821.md`). Spheres are set in the sphere leader's OOB file, not `history/diplomacy`, so the edits are in `history/units/1821/`: Austria now spheres the Papal States and Two Sicilies (the 1821 army of occupation) and holds Sardinia at friendly, France drops to no sphere members at all (correct for 1821 - her client system starts in 1823), and Britain sphered Portugal-Brazil and the Ionian Islands. `Guarantees.txt` is no longer empty: eleven entries for the five-power guarantee of Swiss neutrality, the three-power guarantee of Cracow, Britain over UPB and Austria over PAP/SIC; the clause is unverified in-game and the file header says so.
- Historical 1821 starting literacy for all 521 country tags, replacing the flat `literacy = 0.01` that the 2020 economic-rework commit stamped on every file (472 files changed). Values are assigned by tier so substates, han and princely states always match their overlord (Japanese han = TKG 0.30, princely states = HND 0.05, Chinese successor tags = QNG 0.10); Prussia/Saxony/Scandinavia/Netherlands/Switzerland/UK 0.55, north German minors 0.50, France/Belgium 0.42, Austria and the south German states 0.35, northern Italy 0.25, Spain 0.20, Russia 0.06, Ottomans/Persia/Egypt/India 0.05, uncivilised Africa 0.01-0.03. `non_state_culture_literacy` stays at the 0.01 floor and the dated 1836/1861 blocks stay flat (the engine never reads a block later than the single 1821.9.1 bookmark). Table and justifications in `docs/design/literacy-1821.md`, applied by `scripts/apply_literacy.py`.

### Tooling

- `scripts/refcheck.py` — cross-reference checker: events fired but never defined, orphaned `is_triggered_only` events, missing localisation, undefined modifiers, one-sided flags, unknown names, `on_actions` entries, option counts.
- `scripts/audit_countries.py` — `history/countries` vs `common/countries.txt`: unregistered tags, capitals not owned, undefined/inactive ruling parties, illegal ideologies.
- `scripts/audit_provinces.py` — duplicate province history files, pre-start dated blocks, pops in unowned provinces, regions spanning continents. Regenerates `docs/audit/history-provinces.md`.
- `scripts/audit_diplomacy.py` — `history/diplomacy` and `history/wars` consistency, plus technology/invention modifier keys.
- `scripts/audit_decisions.py` — duplicate decisions, missing `ai_will_do`, unguarded repeatables, duplicate formation paths.
- `scripts/audit_common.py` — `common/` vocabulary and counts against vanilla, invalid modifier keys, duplicate definitions, `defines.lua` diff.
- `scripts/audit_loc.py` — conflicting keys, mojibake, placeholders, malformed rows, missing country names/adjectives.
- `scripts/audit_events.py` — unknown trigger/effect keywords, re-firing events with permanent effects, `year = 1836` gates that lock out the 1821 start.
- `scripts/audit_perf.py` — scores every self-firing event by trigger cost and ranks the hotspots.
- `scripts/balance_factories.py` — factory and artisan margins including maintenance; `--vanilla` for the reference band, `--target R` for the budget solver.
- `scripts/audit_parties.py` — every party block in `common/countries` against `ideologies.txt`, the `party_issues` groups in `issues.txt`, `countries.txt` and `IdeologyEnabling.txt`: ideology/issue/option names, date sanity, per-year coverage 1821-1947, conservative fallback, dead and duplicate parties. Snapshot in `docs/audit/parties.md`.
- `scripts/audit_events2.py` — second-opinion event sweep: `FROM`/`THIS` scope misuse, country effects in province scope, unguarded repeatable permanent modifiers, `major = yes` on non-news events. Snapshot in `docs/audit/events-second-opinion.md`.
- `scripts/audit_fire_once.py` — every self-firing `country_event` with `fire_only_once` and no bare `tag =` / `owns =` test, classed A (an `OR` of tags), B (culture/continent/GP test) or C (no country test). Snapshot and verdicts in `docs/audit/fire-only-once.md`.
- `scripts/audit_pacing.py` — player-facing event load over 1821-1836 for the 19 major playable tags: per tag, the self-firing `country_event`s that can reach it in the window, expected fires per year, `major = yes` popups, same-day cascades and runaway repeaters. Read-only and advisory (exit 0); `--write` regenerates `docs/audit/pacing-1821-1836.md`.
- `scripts/audit_owner_scope.py` - country effects applied in the wrong owner/province scope across all events. Snapshot in `docs/audit/owner-scope.md`; the Afghan chain was its only high.
- `scripts/audit_religion.py` — every religion trigger and effect in the tree against what the pops actually carry: 136 real-religion sites, 124 of them in triggers that can never be true. Snapshot and remediation options in `docs/audit/religion-dead-content.md`.

### Audits (no code change)

- `docs/audit/crises.md` — map of the crisis system: engine-driven flashpoints, the vanilla `20000`-`20105` events the mod keeps almost verbatim, and the mod's own crisis-*suppression* layer `20110`-`20115` (the only scripted tension *source* is 20115). Also the `CRISIS_*`/`TENSION_*` defines against vanilla (only `CRISIS_BASE_CHANCE` 20 -> 10 and `TENSION_ON_REVOLT` 50 -> 40 differ) and the `crisis = yes` CB list. Three proposals are left open, see Deferred.
- `docs/audit/bridge-1836.md` — do the 1830s set pieces still fire from an 1821 start? Traces six episodes back to something an 1821 game produces. All six are reachable: the Great Trek, the Carlist War, the Texas Revolution, the Canadian rebellions of 1837-38 and the Egyptian-Ottoman wars need only year gates or flags with live event setters, and the 39 country files whose flags are set only inside a dead `1836.1.1` history block turn out to be backfills. Item 2, the First Anglo-Afghan War, is not modelled at all; item 2 also produced the Great Game year gate above.
- `docs/audit/start-wars.md` — the eight wars still running on 1821.9.1 (the `rem_*` lines in `history/wars/*.txt` are dated after the bookmark and are never read). Six are endable by the AI or already have a scripted resolution; the `cut_down_to_size` `peace_cost_factor = 100` trap that froze the rest is what `SPAAyacuchoGVG` and `WarResolutionsGVG` exist to unstick. Kept in sync with the live Ottoman-Persian War (`5760ee67`).

### Docs and tooling upkeep

- `CLAUDE.md` and `.claude/skills/validate/SKILL.md` consolidated after a run of appends (`0a5d815f`): the duplicated GVG id ranges now point at `events/GVG Event IDs.txt` as the single authoritative registry instead of being restated, and the baselines live in the validate skill.
- `.claude/skills/new-event/SKILL.md` pointed at the right path: the start-of-game setup events live in `events/SetupGVG.txt`, not `decisions/` (`b366d3d8`).

### Lessons for scripting in this mod

Engine facts verified while writing the chains above; they cost a rewrite each, so they are
repeated in `CLAUDE.md` under "Scripting pitfalls".

- `fire_only_once = yes` is **once per game, engine-wide** — not once per country. Use a country flag set in every option for anything a second country could also see.
- `month` is **0-indexed**: `month = 8` is September.
- `continent` is a **province-scope** trigger. At country scope it must be wrapped (`capital_scope = { continent = north_america }`); a bare `continent =` in a country trigger silently never matches.
- `treasury` / `money` are fixed-point int32 hundredths, so the usable range is about ±2,147,483. Larger literals wrap and can flip the sign of the effect.
- `NOT = { a b }` is **NOR**, not NAND: it is true only when *every* clause inside is false.
- `on_actions` numbers are **weights, not chances**; the engine fires exactly one event per country per pulse, so adding an entry dilutes every other entry in that list.
- `province_event` must use the block form (`province_event = { id = X days = N }`); the bare `province_event = X` shorthand does not work.
- `random_neighbor_country` does not exist. Use `any_neighbor_country` with a `random_country` / flag pattern instead.
- `efficiency` goods in `common/production_types.txt` are **daily maintenance per factory level**, bought on top of `input_goods` — not an efficiency multiplier.
- **Pops carry no real religion.** The pop `religion` field holds the culture (or sub-culture) name — `north_german`, `russian`, `french`, `ashkenazi` — and the true religion is commented out beside it (`religion = north_german #religion = protestant`). So `has_pop_religion = orthodox`, `religion = catholic` and `pop_majority_religion = protestant` match nothing anywhere in the mod; test cultures instead, and use the religion form only for the sub-culture names.
- `sed -i` on this machine rewrites files with LF. Never use it on `CoE_RoI_R/**` (`.txt` must stay CRLF); use Python with `newline=''`.

### Removed files

- Fully commented-out event files, with their id ranges recorded as reserved in `events/GVG Event IDs.txt`: `+demand.txt`, `industrial_demand.txt` (also a latent duplicate-id crash on 999971-999999), `+education_RGO1.txt`, `+education_RGO2.txt`, `0_demographic_transition.txt`, `0_demographic_transition2.txt`, `population trends.txt`.
- `events/money.txt` — a debug handout with an id past the int32 event-id range and a `TAG = HAW` line that is not a condition.
- `decisions/SetupGVG.txt` — a stale duplicate of `events/SetupGVG.txt`.
- `history/wars/EgyptianConquestofSudan.txt` and the four duplicate province files listed above.

### Play-test checklist

Nothing below has been run in a game. In the first in-game years, watch:

- `E:\OneDrive\Documents\Paradox Interactive\Victoria II\CoE_RoI_R\logs\error.log` for unknown effects/scopes coming from the new GVG files (`ENGReformGVG`, `TURAuspiciousGVG`, `ZollvereinGVG`, `PORMiguelistGVG`, `RUSFlavor` 95073). Note the log's size before launching.
- The quarterly economy pulse actually running: some great power must hold `economy_pulse_host`. In the console, `tag <GP>` and check the flag (or watch for 99997's effects each quarter); the election 2000100 is now self-healing, so if no country ever holds it the trigger is wrong.
- The education ladder: `RGO_education_N` modifiers should appear on states within two quarters of the start, now that 9999959-9999969 are dispatched per state from the pulse rather than daily.
- `heavy_factory` profitability at the 1821 start — the 376 existing levels are the whole point of the maintenance rescale; if they still bleed, see `docs/design/factory-balance.md`.
- The Decembrist chain firing in November-December 1825, the Brussels riots after the July Revolution in 1830, and `found_the_zollverein` showing up for Prussia from 1833.
- The Spanish American wars should end by ~1826-1830 via the Ayacucho events; check SPA is at peace by 1831.

### Deferred / design questions

- **Flat literacy.** Every one of the 521 history files starts at `literacy = 0.01`, and the dated 1836/1861 blocks were flattened too (commit `8f5e1248`). No script effect in Victoria 2 can set pop literacy, so this is a deliberate design, not a bug — recorded in `docs/audit/ai-balance.md` rather than reverted. Open question: whether the great powers should still start with a literacy edge.
- **Empty spheres and guarantees at start.** `history/diplomacy/Guarantees.txt` is empty and no `sphere` entry exists anywhere, so the Vienna settlement has no mechanical backing at 1821. See `docs/audit/diplomacy-tech.md`.
- **1031 conflicting localisation keys** across the csvs (the alphabetically first file wins). Only the winner rows were fixed this pass; the duplicates themselves stand. `docs/audit/localisation.md`.
- **39 history/countries files for tags not registered in `common/countries.txt`** (BMK, DUR, ERT, KRL, KYR, ...) plus the REB placeholder. Either register or delete; `audit_countries.py` reports them as `[high]` until then.
- **Abandoned events** — 14 `is_triggered_only` events nothing ever fires, kept on purpose. Baseline in `.claude/skills/validate/SKILL.md`.
- **Performance hotspots needing a design change, not a reorder**: `+education_RGO.txt` 9999959-9999969, `crises.txt` 20110-20112, Ottoman 31268, `Revolution_Nationalism_Event.txt` 97175, `TemperanceLeague.txt` 130, `BoerWar.txt` 98225/98226, and the `Goods.txt` 1107-1138 dispatch, which needs a per-province pulse that preserves the MTTH rate. `docs/audit/performance.md`, `docs/audit/core-systems.md`.
- **`food_factory` price floor** — at `food_industry` = 0.5 the template emits 400 units for 200 of revenue, so its margin is hostage to one price. Fixing it means moving `goods.txt`, which is a separate pass.
- **`defines.lua` follow-ups.** The four values above are now decided and applied; what is left is the script that was written against the old ones, plus `BASE_COUNTRY_ADMIN_EFFICIENCY = 1.0`, which was raised in the same commit that zeroed `BUREAUCRACY_PERCENTAGE_INCREMENT` and keeps the restored increment inert until it is lowered too. `docs/audit/common.md`, "Follow-ups after the defines change".
- **1821-1836 coverage backlog** (`docs/design/1821-1836-coverage.md`). All ten prioritised additions in that audit are now scripted: Zollverein, the two English reforms, the November Uprising repair, the Auspicious Incident, the Miguelist Wars, the Nullification Crisis, the Monroe Doctrine, the Java War, the Daoguang opium edicts and the Italian risings of 1831 — plus the Russo-Turkish War 1828-29, the First Anglo-Ashanti War and the Austrian Vormärz, which the table listed as missing but the top ten did not cover. Since then the Brazilian Regency (`BRZRegencyGVG`) and Japan's Tenpo era (`JAPTenpoGVG`) have been added as well. The Conquest of Algiers stays as the pre-existing partial (Fan Affair, no landing chain), now bug-fixed rather than extended. Still missing from the table: Argentina's unitarian-federal wars 1828-31, the Peru-Bolivian Confederation 1836-39 (Ayacucho 1824 is now scripted, `SPAAyacuchoGVG`), Denmark 1831 and the Swedish-Norwegian union, the Vila-Francada 1823-24, Mexico's centralist turn (44853 is a single event with no reform effect), the Missouri Compromise, an Egyptian branch for the Morea expedition, Prussian 1831 mobilisation, and dates on the Carlist and Spanish-American chains.
- **Crisis proposals** (`docs/audit/crises.md`): the 20111 `ai = yes` asymmetry is fixed above; the `country_event = 2626` `remove_core` dispatch stays commented out until its `FROM`-scope clause in `CBsAndCores.txt:2130-2133`/`:2191-2194` is verified in game, so foreign cores - and the flashpoints they feed - still accumulate monotonically over a 94-year game. And 30 mod CBs are crisis-capable by default (including `restore_byzantine_empire`) without saying so.
- **Rebels and casus belli** — the `defection = none` changes, the 1-100 `spawn_chance` rebase and the 24 zero-infamy event CBs were traced back to the PDM import and to deliberate, self-documented rework. No change made; see `docs/audit/rebels-cb.md`.
- **Line-by-line legacy reviews**: all large flavour/system/generic event files have had a line-by-line review across the ≈160 commits of this pass; ≈20 small `*Flavor` files remain unreviewed. Reports are `docs/audit/legacy-*.md`; every file reviewed surfaced 3-5 real defects.
- **Religion-based legacy content is dead by design of the pops file.** Because the pop `religion` field carries culture names, every religion trigger inherited from PDM/vanilla — the Irish Catholic chain, TemperanceLeague, the Persian Shiite/Sunni content in `DIM/PERFlavour_five_x.txt`, `MOR.txt`, `Taiping.txt`, the ENGFlavor Hindu/Sunni events — silently never fires. Decide whether to restore real religions to `history/pops/1821.9.1` (and find another home for the sub-cultures) or to strip the religion mechanics outright.
