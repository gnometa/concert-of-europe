political_decisions = {

	DIM_flores_offer = {
		picture = DIM_timor
		potential = {
			tag = NET
			NOT = { has_country_flag = DIM_flores_offer }
			owns = 1413
			owns = 1443
			OR = {
				POR = { owns = 1444 }
				UPB = {	owns = 1444	}
				IBR = { owns = 1444 }
			}
		}
		
		allow = {
			ideological_thought = 1
			war = no
			money = 50000
			owns = 1443
			OR = {
				UPB = {	owns = 1444	}
				POR = { owns = 1444 }
				IBR = { owns = 1444 }
			}
		}
		effect = {
			set_country_flag = DIM_flores_offer
			1444 = {
					province_event = 211300
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	reunite_lan_xang = {
		picture = DIM_timor
		potential = {
			OR = {
				tag = LUA
				tag = CHK
				tag = WIA
			}
			NOT = { exists = LXA }
		}
		
		allow = {
			war = no
			owns = 1356
			owns = 1357
			owns = 1361
			owns = 1362
			owns = 1363
		}
		
		effect = {
			prestige = 10
			set_country_flag = laos_formed
			random_owned = {
				limit = { owner = { tag = LUA } }
				owner = { set_country_flag = Luangprabang }
			}
			random_owned = {
				limit = { owner = { tag = CHK } }
				owner = { set_country_flag = Champasak }
			}
			random_owned = {
				limit = { owner = { tag = WIA } }
				owner = { set_country_flag = Wiangchhan }
			}
			change_tag = LXA
			1356 = { add_core = LXA }
			1357 = { add_core = LXA }
			1361 = { add_core = LXA }
			1362 = { add_core = LXA }
			1363 = { add_core = LXA }
		}
		
		ai_will_do = {
			factor = 1
		}
	}
	
}
